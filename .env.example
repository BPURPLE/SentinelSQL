# Application environment
SENTINELSQL_ENV=development

# Authentication (provide securely outside source control)
JWT_SECRET=
JWT_ALGORITHM=HS256

# Database connectivity (least-privileged gateway account only)
DATABASE_URL=

# Optional ML and LLM provider configuration
ML_MODEL_PATH=
LLM_API_KEY=
