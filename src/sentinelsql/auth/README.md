# Authentication and authorization

Contains JWT validation and role-based access control. Supported roles are `ADMIN`, `ANALYST`, and `USER`. Authentication remains separate from business and detection logic.
